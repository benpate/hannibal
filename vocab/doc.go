// Package vocab defines the commonly used constants from the ActivityStreams
// Vocabulary, along with simple validation helpers that confirm a string matches
// a designated type. Other Hannibal packages use it to keep the right values in
// the right places.
package vocab
